#include <stdio.h>
int main()    
{      
    printf("192372128 / Revanth.\n"); 
    int arr[] = {3,4,3,6,6,2,2,7};       
    int length = sizeof(arr)/sizeof(arr[0]);    
    printf("Duplicate elements in given array: \n");
    for(int i = 0; i < length; i++) 
	{    
        for(int j = i + 1; j < length; j++) 
		{    
            if(arr[i] == arr[j])    
                printf("%d\n", arr[j]);    
        }    
    }    
    return 0;    
}
